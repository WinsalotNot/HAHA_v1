<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner.Underlined">
        <item name="android:dropDownHorizontalOffset">0dp</item>
        <item name="android:dropDownVerticalOffset">0dp</item>
        <item name="android:spinnerMode">dropdown</item>
        <item name="android:popupBackground">@android:color/white</item>
        <item name="android:indicatorLeft">0dp</item> <!-- Remove dropdown arrow -->
    </style>

    <style name="CustomMaterialDatePickerTheme" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize the date picker background color -->
    </style>
</resources>